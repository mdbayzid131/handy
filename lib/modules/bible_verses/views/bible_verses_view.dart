import 'package:flutter/material.dart';

class BibleVersesView extends StatelessWidget {
  const BibleVersesView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Bible Verses')));
  }
}
