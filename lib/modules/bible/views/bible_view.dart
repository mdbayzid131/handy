import 'package:flutter/material.dart';

class BibleView extends StatelessWidget {
  const BibleView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Bible')));
  }
}
