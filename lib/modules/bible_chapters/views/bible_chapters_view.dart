import 'package:flutter/material.dart';

class BibleChaptersView extends StatelessWidget {
  const BibleChaptersView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Bible Chapters')));
  }
}
